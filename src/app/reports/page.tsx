import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ReportsPage() {
  return (
    <PlaceholderPage
      title="Reports"
      subtitle="Simple owner reports for occupancy, revenue, reviews and marketing."
      items={["Weekly summary", "Monthly report", "Export-ready notes"]}
    />
  );
}
