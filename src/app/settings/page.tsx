import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      subtitle="Property details, manual data preferences and future integrations."
      items={["Property profile", "Data settings", "Integration readiness"]}
    />
  );
}
