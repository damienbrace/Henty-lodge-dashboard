import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function BookingsPage() {
  return (
    <PlaceholderPage
      title="Bookings"
      subtitle="Calendar and stay list for manual booking management."
      items={["Upcoming stays", "Room availability", "Manual booking form"]}
    />
  );
}
