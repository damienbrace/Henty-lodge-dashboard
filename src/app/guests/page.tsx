import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function GuestsPage() {
  return (
    <PlaceholderPage
      title="Guests"
      subtitle="Guest directory, notes, tags and stay history."
      items={["Guest profiles", "Preferences", "Repeat-stay notes"]}
    />
  );
}
