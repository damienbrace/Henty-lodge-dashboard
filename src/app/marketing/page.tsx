import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function MarketingPage() {
  return (
    <PlaceholderPage
      title="Marketing & AI"
      subtitle="Marketing snapshot, campaign ideas and editable AI drafts."
      items={["Booking gaps", "Campaign drafts", "Content ideas"]}
    />
  );
}
