import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ReviewsPage() {
  return (
    <PlaceholderPage
      title="Reviews"
      subtitle="Ratings, guest feedback and review request follow-ups."
      items={["Recent reviews", "Pending requests", "Reply drafts"]}
    />
  );
}
